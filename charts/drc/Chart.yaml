apiVersion: v2
name: drc
description: Docker Registry Cleaner - Retention policy and garbage collection for Docker registries
type: application
# Release CI replaces these placeholders with the stable app tag version, e.g. v0.3.0 -> 0.3.0.
version: 0.0.0
appVersion: "0.0.0"
